<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
      <a class="navbar-brand" href="/">EJS Basic</a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link <%= activePage === 'home' ? 'active' : '' %>" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= activePage === 'syntax' ? 'active' : '' %>" href="/syntax">Syntax</a>
           <li class="nav-item">
            <a class="nav-link <%= activePage === 'student' ? 'active' : '' %>" href="/student">Student</a>
           </li>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <main class="container my-5">
    <h1><%= title %></h1>

    <% if (topics && topics.length) { %>
      <ul class="list-group">
        <% topics.forEach(topic => { %>
          <li class="list-group-item"><%= topic %></li>
        <% }); %>
      </ul>
    <% } else { %>
      <p>No topics available.</p>
    <% } %>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>